# task-trail
A terminal-focused task manager that persists JSON-formatted records in user space, executes command dispatch via subcommands, and handles state mutations through explicit flag evaluations.
